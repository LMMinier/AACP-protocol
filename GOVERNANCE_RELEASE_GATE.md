# Governance Release Gate

Do not deploy an AI agent with tools unless every context segment has authority metadata, every risky sink is classified, memory writes are gated, model outputs are re-validated, logs are hash-chained, and blocked incidents become regression tests.
